# Generated by expo/expo-router at dev time. The root .gitignore does not know
# about these, and `expo start` writes all of them.
.expo/
expo-env.d.ts

# Prebuild output. This app is managed-workflow only; if these appear, someone
# ran `expo prebuild` and the native projects are disposable.
/ios
/android
